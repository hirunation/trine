# Build artifacts
build/
*.o
*.a

# Raw downloaded datasets (large, reproducible via fetch scripts)
data/raw/

# Prepared JSONL (reproducible via prepare scripts)
data/prepared/

# Splits (reproducible via split_data.py)
data/splits/

# OS files
.DS_Store
*.swp
*~
